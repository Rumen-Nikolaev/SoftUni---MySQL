# SQL Query Task: Count Records in Wizard Deposits

## Objective
Count the total number of entries in the `wizzard_deposits` table.

## SQL Query

```sql
SELECT COUNT(id) AS count
FROM wizzard_deposits;
